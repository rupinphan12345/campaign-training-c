#include <stdio.h>
#include <stdlib.h>

//Cho 2 số nguyên a và b, so sánh 2 số

int main() {
    int a, b;
    printf("nhap a: ");
    scanf("%d", &a);
    printf("nhap b: ");
    scanf("%d", &b);

    if(...){
        printf(...);
    } else if (...) {
        printf(...);
    }else {
        printf(...);
    }

    return 0;
}