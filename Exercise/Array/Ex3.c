#include <stdio.h>
#include <stdlib.h>

//Cho một mảng số nguyên A có n phần tử, tìm giá trị lớn nhất có trong mảng.

int main() {
    int n;
    printf("nhap n: ");
    scanf(...);
    int A[...];

    for(...; ...; ....) {
        printf("nhap phan tu thu %d: ", ....);
        scanf("%d", &A[...]);
    }

    for(...; ...; ....) {
        ...
    }
    
    printf("%d", ...);

    return 0;
}