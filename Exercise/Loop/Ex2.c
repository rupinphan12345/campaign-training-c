#include <stdio.h>
#include <stdlib.h>

//Tính trung bình cộng các số nguyên dương trong khoảng 0 < x < n

int main() {
    int n;
    printf("nhap n: ");
    scanf("%d", &n);

    while(...) {
        ...
    }
    
    printf(...);

    return 0;
}