#include <stdio.h>
#include <stdlib.h>

//Cho số nguyên a, kiểm tra a là số chẵn hoặc lẽ

int main() {
    int a;
    printf("nhap a: ");
    scanf("%d", &a);

    if(...){
        printf(...);
    }else {
        printf(...)
    }

    return 0;
}